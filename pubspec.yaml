name: sudoku
description: "A Sudoku Game made with Flutter."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  logger: ^2.7.0
  path: ^1.9.1
  rxdart: ^0.28.0
  path_provider: ^2.1.5
  markdown_widget: ^2.3.2+8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/help.md
